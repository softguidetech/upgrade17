<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="taj_invoice.list">
      <field name="name">taj_invoice list</field>
      <field name="model">taj_invoice.taj_invoice</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="taj_invoice.action_window">
      <field name="name">taj_invoice window</field>
      <field name="res_model">taj_invoice.taj_invoice</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="taj_invoice.action_server">
      <field name="name">taj_invoice server</field>
      <field name="model_id" ref="model_taj_invoice_taj_invoice"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="taj_invoice" id="taj_invoice.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="taj_invoice.menu_1" parent="taj_invoice.menu_root"/>
    <menuitem name="Menu 2" id="taj_invoice.menu_2" parent="taj_invoice.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="taj_invoice.menu_1_list" parent="taj_invoice.menu_1"
              action="taj_invoice.action_window"/>
    <menuitem name="Server to list" id="taj_invoice" parent="taj_invoice.menu_2"
              action="taj_invoice.action_server"/>
    -->
  </data>
</odoo>